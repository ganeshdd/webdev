let name="ganesh"
let age=21
console.log(`my name is ${name.toUpperCase()} and my age is ${age}`);//use thsis syntax with back ticks


const Gitname= new String("ganesh");//many string methods can be acessed and this type of syntax uses object
console.log(Gitname[4]);
console.log(Gitname.length)
const newstring=Gitname.substring(0,4)
console.log(newstring);